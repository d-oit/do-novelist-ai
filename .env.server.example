# Environment Configuration for Serverless API Gateway
# This file contains server-side only environment variables
# These variables are NOT exposed to client builds

# =============================================================================
# OPENROUTER CONFIGURATION (SERVER-SIDE ONLY)
# =============================================================================
# OpenRouter API Key - This is a server-side only environment variable
# It will NOT be bundled into client builds
OPENROUTER_API_KEY=your_openrouter_api_key_here

# AI Configuration (Server-side overrides if needed)
# These are primarily used for client-side configuration
# Server-side uses them as defaults
VITE_DEFAULT_AI_PROVIDER=nvidia
VITE_DEFAULT_AI_MODEL=nvidia/nemotron-3-nano-30b-a3b:free
VITE_THINKING_AI_MODEL=anthropic/claude-3-5-sonnet-20241022
VITE_ENABLE_AUTO_ROUTING=true
VITE_ENABLE_MODEL_VARIANTS=true

# =============================================================================
# SERVERLESS CONFIGURATION
# =============================================================================
# Rate limiting configuration (server-side)
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_REQUESTS_PER_HOUR=500

# Cost tracking configuration (server-side)
COST_LIMIT_MAX_PER_USER_PER_MONTH=5.0
COST_LIMIT_ALERT_THRESHOLD=0.8

# Cache configuration (server-side)
CACHE_TTL_SECONDS=3600
