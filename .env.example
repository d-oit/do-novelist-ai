# AI Provider Configuration
# This application uses Vercel AI Gateway for multi-provider routing
# See: https://vercel.com/docs/ai-gateway

# =============================================================================
# ENVIRONMENT VARIABLE TEMPLATE FILE
# =============================================================================
# ⚠️  IMPORTANT: This is an EXAMPLE/TEMPLATE file with placeholder values only
# All values starting with "your_" are placeholders and MUST be replaced with real values
# DO NOT use these placeholder values in production
# This file is safely committed to version control as it contains no real secrets

# =============================================================================
# VERCEL AI GATEWAY (REQUIRED)
# =============================================================================
# Single API key for all providers via Vercel AI Gateway
# Get your key at: https://vercel.com/dashboard/ai-gateways
# Then configure provider keys (OpenAI, Anthropic, Google, Mistral) in the Gateway dashboard
VITE_AI_GATEWAY_API_KEY=your_ai_gateway_api_key_here

# Turso Database Configuration
VITE_TURSO_DATABASE_URL=your_turso_database_url_here
VITE_TURSO_AUTH_TOKEN=your_turso_auth_token_here

# AI Configuration
# Default provider: mistral (recommended), openai, anthropic, or google
VITE_DEFAULT_AI_PROVIDER=mistral

# Model Configuration
# Thinking/Reading Model (complex tasks): mistral:mistral-medium
# Default Model (standard tasks): mistral:mistral-medium-latest
# Fast Model (simple tasks): mistral:mistral-small-latest
VITE_DEFAULT_AI_MODEL=mistral:mistral-medium-latest
VITE_THINKING_AI_MODEL=mistral:mistral-medium

# Cost and Usage Limits
VITE_MONTHLY_AI_BUDGET=10
VITE_ENABLE_AUTO_FALLBACK=true
VITE_ENABLE_COST_OPTIMIZATION=true
